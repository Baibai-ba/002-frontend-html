# 🚀 Git 常用操作速查表

## 第一次上传（新建仓库时）
git init
git add .
git commit -m "init: first commit"
git branch -M main
git remote add origin https://github.com/你的用户名/仓库名.git
git push -u origin main

## 后续更新（常用 3 步）
git add .
git commit -m "update: 本次改动说明"
git push
